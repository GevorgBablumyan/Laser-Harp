# Laser-Harp
Arduino-based MIDI controller using 8 light sensors (LDRs) as touchless keys. Each sensor triggers musical notes, while an ultrasonic sensor controls modulation (CC). Supports dynamic MIDI channels via potentiometer input—creating a gesture-based, contactless musical instrument.
